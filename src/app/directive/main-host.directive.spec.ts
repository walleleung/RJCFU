import { MainHostDirective } from './main-host.directive';

describe('MainHostDirective', () => {
  it('should create an instance', () => {
    const directive = new MainHostDirective();
    expect(directive).toBeTruthy();
  });
});
